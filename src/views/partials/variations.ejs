<div class="<%= variationsName %>-list row">
    <% variations.map((variation) => { 
        variation.map((item) => { %>
            <div class="<%= variationsName %>-item col-4">
                <a href="/<%= item.wd %>"><%= item.wd %></a> 
            </div> 
        <% }); %>
    <% }); %>
</div>